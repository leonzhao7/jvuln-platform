Generate configuration and business code for a Spring Boot demo that can be exploited via the following CVE.

## CVE Intelligence
{{intelligence}}

## Trigger Chain (how the vulnerability is triggered)
{{trigger_chain}}

## Root Cause Analysis
{{root_cause}}

## Patch Diff (showing what was fixed)
{{patch_diff}}

## Affected Component
Maven coordinates: {{artifact}}

## Existing Project Skeleton
The following files already exist and MUST NOT be regenerated:

**pom.xml** — Spring Boot 2.7.18 parent, Java 8. The vulnerable version of the affected library is already declared as a dependency (the version has been pinned to a known-vulnerable release). You do not need to worry about dependency versions.

**src/main/java/com/jvuln/demo/Application.java** — Standard @SpringBootApplication main class.

**build.sh** / **run.sh** — Build and launch scripts (port 18080).

## Your Task
Generate ONLY the additional files needed so that the vulnerability can be triggered from outside:

1. **Configuration**: Classes or properties that enable the vulnerable code path in the library. For example, if the CVE is in Tomcat's DefaultServlet, configure the embedded Tomcat to expose a writable DefaultServlet and enable file-backed session persistence. If the CVE is in a parser or deserializer, ensure the framework routes user input through the vulnerable parser.

2. **Business code**: Write 1–2 simple, realistic controllers that a normal application would have. These controllers should NOT simulate the vulnerability — they exist to make the application look and behave like a real web app. The vulnerability is triggered by sending requests to the LIBRARY's own endpoints or by exploiting how the library processes requests.

3. **Resource files**: application.properties, static resources, or templates if the vulnerability requires them.

IMPORTANT: The vulnerability exists in the library itself. Your code should configure and use the library, NOT reimplement its internal behavior. A PoC exploit will target the library's endpoints or processing pipeline, not your controllers (unless the CVE is in application-level code like a web framework's parameter binding).
