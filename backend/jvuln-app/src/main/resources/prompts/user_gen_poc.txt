Generate a Proof-of-Concept script for the following vulnerability:

## CVE Intelligence
{{intelligence}}

## Trigger Chain
{{trigger_chain}}

## Vulnerable Demo Application
The vulnerable Spring Boot application is running at: http://localhost:18080
{{vuln_demo_info}}

The application is a normal Spring Boot web app. The vulnerability exists in the underlying library/container (see the CVE intelligence above), NOT in the application's own controllers. Your PoC should exploit the library's vulnerable code path directly.

## Your Task
Create a PoC script that:
1. Exploits the vulnerability by sending requests that reach the library's vulnerable code path (e.g., Tomcat's DefaultServlet, a deserialization endpoint, an expression evaluator)
2. Performs a harmless but verifiable action (write a marker file through a vulnerable path, read protected data, trigger benign deserialization, etc.)
3. Includes clear educational comments explaining each exploitation step
4. Does NOT rely on any custom simulation endpoints in the application — the exploit must work against the library itself

Also provide a verification command that:
- Returns exit code 0 if the exploit succeeded
- Returns non-zero exit code if it failed
- Should be a simple one-liner (curl + grep, test -f, etc.)
